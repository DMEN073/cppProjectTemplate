# cppProjectTemplate
This is a project template for cpp project with cmake build system

# Setup
1) Install poetry
2) Run poetry install in project root directory to install all the needed python package
3) poetry run python -m buildtool.build configure
4) poetry run python -m buildtool.build build